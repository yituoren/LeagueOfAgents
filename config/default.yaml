# League of Agents Default Configuration

# Global LLM config (used as default for all players)
llm:
  model: "gpt-4o-mini"
  base_url: null
  api_key: "AIzaSyCGy_LFjtEe5iJXmdJV9_hh-nlnGGRDWZ8" # Falls back to OPENAI_API_KEY env var if null
  temperature: 0.7
  max_tokens: 2048

game:
  name: "draw_and_guess"
  num_rounds: 4
  max_steps_per_round: 10
  timeout_seconds: 30.0

# Each player can override global llm config with its own llm section
players:
  - name: "Alice"
    # llm:               # Uncomment to override for this player
    #   model: "gpt-4o"
    #   base_url: null
    #   api_key: null
  - name: "Bob"
  - name: "Charlie"
  - name: "Diana"

# Tool configurations
tools:
  image_generation:
    model: "nano-banana"
    base_url: null
    api_key: null

# Game-specific settings
draw_and_guess:
  word_pool:
    - "Apple"
    - "Sun"
    - "Moon"
    - "Cat"
    - "Airplane"
    - "Snowflake"
    - "Guitar"
    - "Castle"
    - "Rainbow"
    - "Robot"
    - "Volcano"
    - "Diamond"
    - "Butterfly"
    - "Telescope"
    - "Lighthouse"

logging:
  level: "INFO"
  output_dir: "logs"
