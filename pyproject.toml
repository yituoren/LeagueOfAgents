[project]
name = "league-of-agents"
version = "0.1.0"
description = "A Multi-Agent Gaming Framework"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
]
